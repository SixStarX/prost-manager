# Copie para prost-web/.env.local e preencha se for usar o Sentry no frontend.
# Variáveis do Vite precisam do prefixo VITE_ para chegar ao bundle.

# Sentry (error tracking) — opcional. Sem DSN, o Sentry nem é carregado no bundle.
# VITE_SENTRY_DSN=
# VITE_SENTRY_TRACES_SAMPLE_RATE=0
